import { DebugView } from "./debug-view";

export const metadata = {
  title: "Debug Assistido — Brain",
  description: "Cole o erro, receba diagnóstico de causa raiz com fix acionável.",
};

export default function DebugPage() {
  return <DebugView />;
}
